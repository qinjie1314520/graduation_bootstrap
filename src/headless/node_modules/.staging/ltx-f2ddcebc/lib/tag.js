'use strict'

var tagString = require('.staging/ltx-f2ddcebc/lib/tagString')
var parse = require('.staging/ltx-f2ddcebc/lib/parse')

module.exports = function tag (/* [literals], ...substitutions */) {
  return parse(tagString.apply(null, arguments))
}
