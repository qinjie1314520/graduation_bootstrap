'use strict';
var $              = require('.staging/core-js-03f9c524/modules/$')
  , descriptor     = require('.staging/core-js-03f9c524/modules/$.property-desc')
  , setToStringTag = require('.staging/core-js-03f9c524/modules/$.set-to-string-tag')
  , IteratorPrototype = {};

// 25.1.2.1.1 %IteratorPrototype%[@@iterator]()
require('.staging/core-js-03f9c524/modules/$.hide')(IteratorPrototype, require('.staging/core-js-03f9c524/modules/$.wks')('iterator'), function(){ return this; });

module.exports = function(Constructor, NAME, next){
  Constructor.prototype = $.create(IteratorPrototype, {next: descriptor(1, next)});
  setToStringTag(Constructor, NAME + ' Iterator');
};