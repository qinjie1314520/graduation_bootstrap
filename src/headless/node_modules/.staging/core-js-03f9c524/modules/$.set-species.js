'use strict';
var global      = require('.staging/core-js-03f9c524/modules/$.global')
  , $           = require('.staging/core-js-03f9c524/modules/$')
  , DESCRIPTORS = require('.staging/core-js-03f9c524/modules/$.descriptors')
  , SPECIES     = require('.staging/core-js-03f9c524/modules/$.wks')('species');

module.exports = function(KEY){
  var C = global[KEY];
  if(DESCRIPTORS && C && !C[SPECIES])$.setDesc(C, SPECIES, {
    configurable: true,
    get: function(){ return this; }
  });
};