// 19.1.2.5 Object.freeze(O)
var isObject = require('.staging/core-js-03f9c524/modules/$.is-object');

require('.staging/core-js-03f9c524/modules/$.object-sap')('freeze', function($freeze){
  return function freeze(it){
    return $freeze && isObject(it) ? $freeze(it) : it;
  };
});