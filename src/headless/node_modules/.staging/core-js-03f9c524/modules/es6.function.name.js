var setDesc    = require('.staging/core-js-03f9c524/modules/$').setDesc
  , createDesc = require('.staging/core-js-03f9c524/modules/$.property-desc')
  , has        = require('.staging/core-js-03f9c524/modules/$.has')
  , FProto     = Function.prototype
  , nameRE     = /^\s*function ([^ (]*)/
  , NAME       = 'name';
// 19.2.4.2 name
NAME in FProto || require('.staging/core-js-03f9c524/modules/$.descriptors') && setDesc(FProto, NAME, {
  configurable: true,
  get: function(){
    var match = ('' + this).match(nameRE)
      , name  = match ? match[1] : '';
    has(this, NAME) || setDesc(this, NAME, createDesc(5, name));
    return name;
  }
});