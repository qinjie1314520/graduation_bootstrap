// https://gist.github.com/WebReflection/9353781
var $          = require('.staging/core-js-03f9c524/modules/$')
  , $export    = require('.staging/core-js-03f9c524/modules/$.export')
  , ownKeys    = require('.staging/core-js-03f9c524/modules/$.own-keys')
  , toIObject  = require('.staging/core-js-03f9c524/modules/$.to-iobject')
  , createDesc = require('.staging/core-js-03f9c524/modules/$.property-desc');

$export($export.S, 'Object', {
  getOwnPropertyDescriptors: function getOwnPropertyDescriptors(object){
    var O       = toIObject(object)
      , setDesc = $.setDesc
      , getDesc = $.getDesc
      , keys    = ownKeys(O)
      , result  = {}
      , i       = 0
      , key, D;
    while(keys.length > i){
      D = getDesc(O, key = keys[i++]);
      if(key in result)setDesc(result, key, createDesc(0, D));
      else result[key] = D;
    } return result;
  }
});