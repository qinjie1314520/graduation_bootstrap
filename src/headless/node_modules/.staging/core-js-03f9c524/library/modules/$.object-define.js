var $         = require('.staging/core-js-03f9c524/library/modules/$')
  , ownKeys   = require('.staging/core-js-03f9c524/library/modules/$.own-keys')
  , toIObject = require('.staging/core-js-03f9c524/library/modules/$.to-iobject');

module.exports = function define(target, mixin){
  var keys   = ownKeys(toIObject(mixin))
    , length = keys.length
    , i = 0, key;
  while(length > i)$.setDesc(target, key = keys[i++], $.getDesc(mixin, key));
  return target;
};