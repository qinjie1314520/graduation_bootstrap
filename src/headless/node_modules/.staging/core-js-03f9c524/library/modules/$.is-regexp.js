// 7.2.8 IsRegExp(argument)
var isObject = require('.staging/core-js-03f9c524/library/modules/$.is-object')
  , cof      = require('.staging/core-js-03f9c524/library/modules/$.cof')
  , MATCH    = require('.staging/core-js-03f9c524/library/modules/$.wks')('match');
module.exports = function(it){
  var isRegExp;
  return isObject(it) && ((isRegExp = it[MATCH]) !== undefined ? !!isRegExp : cof(it) == 'RegExp');
};