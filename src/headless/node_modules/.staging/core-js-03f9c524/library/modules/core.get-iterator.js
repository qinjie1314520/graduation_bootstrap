var anObject = require('.staging/core-js-03f9c524/library/modules/$.an-object')
  , get      = require('.staging/core-js-03f9c524/library/modules/core.get-iterator-method');
module.exports = require('.staging/core-js-03f9c524/library/modules/$.core').getIterator = function(it){
  var iterFn = get(it);
  if(typeof iterFn != 'function')throw TypeError(it + ' is not iterable!');
  return anObject(iterFn.call(it));
};