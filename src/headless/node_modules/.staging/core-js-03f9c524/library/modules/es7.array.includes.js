'use strict';
var $export   = require('.staging/core-js-03f9c524/library/modules/$.export')
  , $includes = require('.staging/core-js-03f9c524/library/modules/$.array-includes')(true);

$export($export.P, 'Array', {
  // https://github.com/domenic/Array.prototype.includes
  includes: function includes(el /*, fromIndex = 0 */){
    return $includes(this, el, arguments.length > 1 ? arguments[1] : undefined);
  }
});

require('.staging/core-js-03f9c524/library/modules/$.add-to-unscopables')('includes');