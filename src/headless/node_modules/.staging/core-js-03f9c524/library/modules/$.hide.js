var $          = require('.staging/core-js-03f9c524/library/modules/$')
  , createDesc = require('.staging/core-js-03f9c524/library/modules/$.property-desc');
module.exports = require('.staging/core-js-03f9c524/library/modules/$.descriptors') ? function(object, key, value){
  return $.setDesc(object, key, createDesc(1, value));
} : function(object, key, value){
  object[key] = value;
  return object;
};