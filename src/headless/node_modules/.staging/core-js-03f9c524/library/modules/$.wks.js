var store  = require('.staging/core-js-03f9c524/library/modules/$.shared')('wks')
  , uid    = require('.staging/core-js-03f9c524/library/modules/$.uid')
  , Symbol = require('.staging/core-js-03f9c524/library/modules/$.global').Symbol;
module.exports = function(name){
  return store[name] || (store[name] =
    Symbol && Symbol[name] || (Symbol || uid)('Symbol.' + name));
};