// 20.1.2.2 Number.isFinite(number)
var $export   = require('.staging/core-js-03f9c524/library/modules/$.export')
  , _isFinite = require('.staging/core-js-03f9c524/library/modules/$.global').isFinite;

$export($export.S, 'Number', {
  isFinite: function isFinite(it){
    return typeof it == 'number' && _isFinite(it);
  }
});