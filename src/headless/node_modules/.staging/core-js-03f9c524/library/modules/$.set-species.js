'use strict';
var core        = require('.staging/core-js-03f9c524/library/modules/$.core')
  , $           = require('.staging/core-js-03f9c524/library/modules/$')
  , DESCRIPTORS = require('.staging/core-js-03f9c524/library/modules/$.descriptors')
  , SPECIES     = require('.staging/core-js-03f9c524/library/modules/$.wks')('species');

module.exports = function(KEY){
  var C = core[KEY];
  if(DESCRIPTORS && C && !C[SPECIES])$.setDesc(C, SPECIES, {
    configurable: true,
    get: function(){ return this; }
  });
};