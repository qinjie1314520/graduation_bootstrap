// 26.1.6 Reflect.get(target, propertyKey [, receiver])
var $        = require('.staging/core-js-03f9c524/library/modules/$')
  , has      = require('.staging/core-js-03f9c524/library/modules/$.has')
  , $export  = require('.staging/core-js-03f9c524/library/modules/$.export')
  , isObject = require('.staging/core-js-03f9c524/library/modules/$.is-object')
  , anObject = require('.staging/core-js-03f9c524/library/modules/$.an-object');

function get(target, propertyKey/*, receiver*/){
  var receiver = arguments.length < 3 ? target : arguments[2]
    , desc, proto;
  if(anObject(target) === receiver)return target[propertyKey];
  if(desc = $.getDesc(target, propertyKey))return has(desc, 'value')
    ? desc.value
    : desc.get !== undefined
      ? desc.get.call(receiver)
      : undefined;
  if(isObject(proto = $.getProto(target)))return get(proto, propertyKey, receiver);
}

$export($export.S, 'Reflect', {get: get});